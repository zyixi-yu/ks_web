## 1. Worker API

- [x] 1.1 新增 `worker/routes/i18n.ts` 并实现 `GET /api/i18n/search`
- [x] 1.2 从 KV 读取 `ks_gs_new_cn` / `ks_gs_new_en`，解析 `key=value` 并构建内存缓存
- [x] 1.3 实现 SC2 本地化文本清洗函数（`<n/>`→换行，`<d .../>`→`X`，其他标签去除）
- [x] 1.4 实现多关键词 AND + 分页（默认 `pageSize=20`，返回 `total/page/pageSize/items`）
- [x] 1.5 设置 API 响应头：`Cache-Control: no-store`（避免缓存旧搜索结果）

## 2. Web UI

- [x] 2.1 新增“翻译搜索”导航入口与路由页面（例如 `/i18n`）
- [x] 2.2 页面实现搜索表单（中文子串、多关键词）与加载/错误状态
- [x] 2.3 页面实现结果列表展示：`key` + 英文原文 + 中文翻译（支持换行展示）
- [x] 2.4 页面实现分页组件（每页 20，支持上一页/下一页与页码展示）

## 3. 验证与回归

- [x] 3.1 本地通过 `curl` 验证搜索 API（包含单关键词、多关键词、空查询）
- [x] 3.2 手动验证页面功能与样式（结果换行、分页、性能）
